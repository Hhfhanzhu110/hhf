<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exm.demo.yeb.sysuser.mapper.SystemUserMapper">
    <select id="login" resultType="com.exm.demo.yeb.sysuser.domain.SystemUser">
        SELECT *
        FROM system_user
        WHERE login_name = #{username}
          and password = #{password}
    </select>
</mapper>